# Changelog

## Unreleased

- **场景预设**：设置里新增「演播室 / 深海 / 午夜 / 雾面」一键场景，只拧现有旋钮（色相、文字色、磨砂、粒子、辉光），不改壁纸文件；手调后高亮取消。后续方向见 `docs/roadmap.md`
- **浅色模式过一遍**：浅色玻璃太透、香槟描边融进奶油底、次级墨偏灰。现把浅色家族玻璃填充提到约 78–82%、镜面内缘加暖金暗边、描边改更深的青铜 `rgba(122,86,28)`，主/次/三级墨各压一档，气泡同步加厚。深色配方不动
- **性能档**：设置新增「性能 / 均衡 / 炫」。与场景正交——不改色调旋钮，只决定图层是否真跑。性能：流体停在最后一帧、关掉星河/色散/倾斜/辉光；均衡：现默认（流体静帧 + 星河/色散）；炫：流体循环 + 金鲸/网格/音频。`prefers-reduced-motion` 仍优先

## v2.2.5 (2026-08-25)

- **修复：composer 下拉回到家族液态毛玻璃**：模型 / 访问模式 / 子代理弹层原先要么太透（聊天字叠上来），要么被改成近实心平板，脱离顶栏/侧栏/发送栏那套液态玻璃。根因是发送栏自己带着 `backdrop-filter`（融合条还叠了 `isolation`），弹层只能糊住发送栏内部、糊不住聊天正文。现把发送栏（含融合条）的模糊挪到 `::before` 面纱上，并去掉 inputbar 的 isolation；弹层改用与面板相同的 `--dsh-aqua-glass-card-*` 渐变 + 镜面内缘 + 模糊度旋钮，聊天字被毛玻璃化而不是透出来，也不再是一块实心板
- **修复：better-sidebar 终端实心黑底**：终端 wrap 和 xterm viewport 用了不透明的 `--dsw-alias-bg-base`（深色 `#08090B`），盖在已磨砂的工作台面板上读成一块黑。现把这两层底色拆掉，终端透出面板那套家族毛玻璃，ANSI 字色不动
- **修复：任务列表实心底**：会话里的「任务」卡（以及同族的 composer dock 提示条）用 `--dsw-specific-tip` 平板填充、没有毛玻璃，读成一块实心底。现改成与顶栏/发送栏同一套家族玻璃（渐变 + 镜面内缘 + 模糊度旋钮）
- **修复：漏网平板条**：目标条（`--dsw-specific-tip`）、better-sidebar 面板（只有模糊、填充仍是 layer-1 平板）、以及面板里的 tab 条（再叠一层 layer-1）都还没进家族玻璃。现把目标条和面板改成同一套渐变+镜面内缘，tab 条改透明透出面板玻璃；编辑器 / PDF / iframe 仍是内容画布，不动
- **修复：模型下拉提供商标签发黑**：分组标题（`groupTitle`）用 `--dsw-specific-menu` 做 sticky 底板（深色 92% 近黑），叠在家族玻璃上读成一条黑标签。现拆掉这层底板，字色改为主题次级墨（`--dsw-alias-label-secondary`）
- **修复：设置页模型 / API 协议下拉未适配**：设置「模型」里添加提供方时的模型和 API 协议是原生 `<select>`，触发器吃 `--dsw-alias-bg-layer-1` 平板。现改成家族玻璃（渐变 + 镜面内缘 + 模糊度旋钮），右侧箭头保留；弹出的 option 列表仍走系统绘制 + 已有的不透明底板/墨色规则，保证可读

## v2.2.4 (2026-08-22)

- **修复：香槟金按钮文字看不清**：主要/信息类按钮填充是香槟金 `#f4d28a`（浅色），但 `--dsw-alias-label-primary-foreground` 在暗色模式下是白色 `#FFFFFF`——白字压在浅金上对比度仅 ~1.4，几乎看不清（如「保存词库」等按钮）。现把暗色模式的该 token 改为深色墨水 `#15171C`，对比度提升到 ~12（与 `label-primary-inverted` 一致），主要按钮上的文字恢复清晰可读；浅色模式不变（白字在深金 `#9a6f2c` 上本就合格）

## v2.2.3 (2026-08-22)

- **修复：拉高磨砂度仍发黑（根治）**：上一版只封顶了玻璃卡片两个暗色 stop，但 `--dsh-aqua-frost` 乘数（上限 1.4）还被几十处表面 token 消费——磨砂 100% 时工具提示 72%→100%（实心黑）、菜单/气泡 60%→84%、分层表面 55%→77%，这才是全局发黑的来源。现把乘数上限降到 **1.0**（深色表面永不超设计基准），并把磨砂 50→100 的行程改为叠加**白色雾面**（`--dsh-aqua-frost-white`，最高 0.4），于是「更磨砂」读作更雾、更亮，而不是更黑；玻璃卡片同步加了这层白雾顶光

## v2.2.2 (2026-08-22)

- **修复：代码块顶栏仍显示实心黑**：代码块 banner（语言标签 + 复制按钮那行）背后有个 sticky 包裹层，背景用了不透明的 `--dsw-alias-bg-base`（深色模式 `#08090B` 实心黑），盖在半透明 banner 后面读成一块黑条。这层底色原本只为 sticky 滚动时挡住内容，而浮动模式已把 banner 的 sticky 禁用，故直接把它设为透明——banner 与代码块主体恢复半透明玻璃质感，不再有实心黑条

## v2.2.1 (2026-08-22)

- **修复：拉高磨砂度会发黑**：磨砂度乘数 `--dsh-aqua-frost`（最高 1.4）被线性乘进深色玻璃渐变里两个暗色 stop 的 alpha——磨砂 100% 时 `0.70→0.98`、`0.66→0.92`，玻璃从半透明变成近乎实心的近黑，挡住背后的流体/壁纸读成「发黑」。现给两个暗色 stop 加 `min()` 封顶（`0.86` / `0.84`），磨砂越高玻璃越「雾」而不再发黑——顶部的白色 catch-light 与青色 catch-light 仍随磨砂继续提亮，方向是对的；浅色模式不受影响（其渐变是亮色 stop，原本就发白不发黑）

## v2.2.0 (2026-08-22)

- **全局文字颜色切换**：新增「文字颜色」预设开关（香槟金 / 中性 / 薄荷 / 玫瑰）——一键切换整套界面文本墨色。默认「香槟金」与既有暖金墨色逐字一致（零回归）；「中性」是纯净灰白，「薄荷」「玫瑰」分别带薄荷/玫瑰色调。实现上只覆盖七个 `--dsw-alias-label-*` 墨色 token（primary/secondary/tertiary/caption/dimmed/bluish/primary-dimmed），且每个预设都沿用同一套明度阶梯，深浅两种模式都能保证可读；其余 token 全部走原层，开关关闭时随主题层一起还原

## v2.1.1 (2026-08-21)

- **修复：原生 select 下拉弹层不可读**：模型设置里「提供方」「API 协议」等原生 `<select>` 展开后的选项列表颜色错乱、文字无法阅读——根因是输入框表面 token 是半透明玻璃 fill，option 行继承了墨色却没有背景，Windows Chromium 自绘弹层把透明背景塌缩成浅色底，浅色文字叠上去就消失了（浅色模式则反向：深字叠深底）。现给所有 `option`/`optgroup` 显式设置不透明底板 + 匹配墨色（深色模式暖黑底浅字、浅色模式白底深字），弹层与菜单表面同族；悬停/选中高亮仍由系统绘制

## v2.1.0 (2026-08-21)

- **dsh-better-sidebar 玻璃适配**：为 omdsh-dev/DSH-better-sidebar 插件补玻璃质感——它本身是 token 驱动（面板/标签栏已吃半透明 fill + 香槟描边），但缺 `backdrop-filter`、且工作区 pane 用了不透明 `--dsw-alias-bg-base`（实黑一块）；现给面板/底部面板补上磨砂模糊（跟随「玻璃模糊度」旋钮），并把工作区 pane 改成透明、透出面板磨砂，整个右栏工作台读作一块家族玻璃
- **修复：收起侧栏右侧亮边**：收起后的窄 rail（56px）右缘有一条比主体明显更亮的白色亮带，与其它玻璃格格不入——根因是玻璃色散折射滤镜的位移贴图在 pane 边缘向窗外 ±30px 采样背景，56px 窄列下右边缘把窗外更亮的聊天区流体「拉」进玻璃底下，再叠上香槟边缘光的 screen 混合，形成约 7px 的亮带（展开的 256px 宽侧栏采样落点无落差所以看不出来）。两步根治：先压平窄列的青色 catch-light（`168°` 暗化渐变），再让收起 rail 的 `::before` 面纱豁免色散滤镜（保留磨砂模糊 + 增饱和），亮带完全消除、左右边缘对称；展开侧栏与其它宽面板（顶栏/发送框/轨迹窗）的折射全部保留

## v2.0.1 (2026-08-20)

- **声明 `dsh.bundle` manifest**：新增 `cordis.patch.yml`（注册 `ui-mineradio`），并在 `package.json` 补充 `dsh.bundle.patch` 与 `files`，使主题可通过 `dsh plugin add` 一键安装，无需再手工往 `cordis.patch.yml` 里加补丁；同时修正 `repository` 指向改名后的仓库
- **模糊度/磨砂度覆盖更全**：之前「磨砂度」只控制主玻璃面板，一批「表面 token」小面（设置里的选择器/输入框/下拉菜单/代码块/tooltip 等）透明度是写死的 0.5~0.72，不跟旋钮；现把它们接到磨砂度（`color-mix(... calc(X% * var(--dsh-aqua-frost)) ...)`），磨砂度旋钮会同步缩放这些表面的不透明度。兼容模式的 menu/card/bubble/tooltip/panel 的模糊度也从写死 `12px` 改为跟「玻璃模糊度」旋钮（`blur(var(--dsh-aqua-blur))`）

## v2.0.0 (2026-08-20)

- **氛围镜头（光标视差 + 3D 倾斜）**：流体板（远层）与星河粒子（近层）随光标分层跟随——近层平移约 2×、并带 `perspective` 下的 `rotateX/rotateY` 倾斜与轻微滚转，鼠标扫过时背景像被相机视角掠过，景深感立现；鼠标停下后回到低频正弦「呼吸」漂移。纯 CSS transform、单 rAF、GPU 合成，遵循 `prefers-reduced-motion` 静态化
- **色调改为彩虹色带**：背景「色调」不再是无级角度拉条，改为一条连续彩虹渐变带（红→紫→红），点击/拖动任意位置即取任意色相，滑块实心显示当前颜色；「颜色深浅」滑条独立保留
- **修复**：系统开启「减少动态效果」时，流体着色器单帧静态渲染后换色不重绘（必须刷新才变色）——现在换色会即时补绘一帧
- **玻璃色散滤镜**：把 Mineradio 播放器的 SVG 玻璃色散移植进主题，并拆成两部分——无色折射（位移贴图真实扭动背板，强度约原版）＋可调色边缘光（单一色相、低透明度、只出现在玻璃边缘，默认香槟金，不再叠出紫/品红）；新增「色散颜色」彩虹色带设置；套在顶栏、侧边栏、轨迹窗、发送框、数据栏这几块玻璃上（`blur()` 之后叠加 `url(#mineradio-glass-dispersion)`），禁用时自动退回纯模糊（仅 Chromium 生效）
- **液态玻璃质感**：向 Apple Liquid Glass 靠拢——玻璃加上标志性镜面高光（1px 锐利内缘 + 顶部高光 + 底部厚度线），高光随光标偏移（光标靠近哪条边，那条边就更亮、对边变暗），折射贴图换成凸透镜场并压低强度（中心清晰、边缘微凸放大），折射像更贴近本体，观感更接近真实透镜；折射强度做成「折射强度」旋钮（0–100%，默认 60）可实时调节；侧边栏改用竖版位移贴图并去掉金色右边框，折射带宽度与边缘高光跟其它玻璃完全一致
- **壁纸自动取色**：选自定义壁纸（图片或视频首帧）后，用 canvas 采样主色相，自动驱动聚光灯辉光、环境光晕、色散边缘色，让整套强调色跟随壁纸同色系；加「自动取色」开关——开=跟随壁纸，关=用手动调的色。纯前端、无权限，跨源/纯灰壁纸自动回退默认色
- **磨砂遮罩（壁纸可读性）**：浅色壁纸会跟白色文字糊在一起，加「磨砂遮罩」开关——开启后给壁纸加一层更强模糊 + 半透明磨砂 scrim（深色模式压暗、浅色模式提亮）保证文字可读，遮罩模糊度、遮罩透明度两个旋钮可调；开启时同时静音星河粒子动效，背景安静如雾面玻璃
- **修复**：壁纸切回流体时背景变黑、必须刷新才恢复——根因是壁纸模式下流体 canvas 用 `display:none` 隐藏，`clientWidth/Height` 归零导致 WebGL 绘图缓冲被销毁；改为 `visibility:hidden`（保留布局盒）并在着色器里跳过 0 尺寸重设，缓冲跨切换保持温热
- **粒子密度 + 拖拽**：星河粒子加「粒子密度」旋钮（0–100%，默认 60，最大 2× 默认场，上限 1400 粒）；支持像 Mineradio 那样拖拽粒子——光标移动时附近粒子被顺着拖拽方向卷走（悬停轻搅、按住左键强拉），松手后粒子在阻尼下缓缓流回星带；点击涟漪动效保留不变
- **设置页玻璃统一**：设置弹窗原来是「扁平半透明 + 固定 50px 模糊」的简化玻璃，跟顶栏/侧边栏/发送框的渐变镜面不一致；现改成同一套配方（渐变填充 + 液态玻璃镜面内缘 + 香槟/青 hairline 描边 + 20px 圆角 + 跟随模糊度旋钮），弹窗读作一块家族玻璃而非原生面板
- **修复**：侧边栏内「添加工作区」悬浮提示文字被截断——根因是侧边栏的 `backdrop-filter`（以及悬停 3D 倾斜的 `transform`）让 `position:fixed` 的 tooltip 被重锚定进 260px 的侧边栏、再被 `overflow:hidden` 裁掉右半；把侧边栏模糊挪到 `::before` 遮罩层上（不再构成 fixed 后代包含块），并让侧边栏永久跳过 3D 倾斜（保留辉光），tooltip / 下拉菜单 / 设置弹窗恢复视口定位不再被裁
- **修复**：「Think」标签（及整段对话文字）带描边阴影——根因是主题给对话文字加了一层 `text-shadow` 光晕（深色模式黑色、浅色模式白色），叠在文字上读成描边/黑边；现两种模式都去掉这层文字光晕（`text-shadow` 不再设置），文字恢复干净无描边
- **修复**：正在生成（流式输出）的「Think」/工具行整行套着一圈黑底（像卡片边框）——根因是 DSH 的「reasoning row sweep」流式微光（`data-state='running'` 时的 `::after` 扫光）用了 `var(--dsw-alias-bg-base)`，而在本深色主题里该 token 是近黑色 `#08090B`，扫光因此变成一条黑色影带；现把它改染成柔和的香槟金微光（读作柔和高光而非黑卡），只影响流式那一行、生成完毕即消失
- **侧边栏液态玻璃重构**：把原先「香槟/青 hairline 描边 + 内偏 1px 的白色 inset 内缘环」这套双线边缘，改成**镜面内缘即描边本身**——1px 白色 hairline 贴在玻璃最外缘、顶部更亮（catch-light），去掉内偏 1px 的白环（那正是「白环离边框太远、格格不入」的来源），只保留投影 + 青辉光 + 底部厚度暗部，边缘读作一条锐利的白色 rim 而非两条叠线
- **音频响应**：新增「音频响应」开关（默认关）——用 Web Audio API 采集麦克风音频做频谱分析，**只让星河粒子随音乐律动**（中高频能量调亮调大粒子，带快攻慢放的自适应峰值归一化包络，无论采到多小的音量都能满幅摆动）；流体与辉光不随音频变化（避免整块背景跟着鼓点涌动读成噪声）；`prefers-reduced-motion` 下禁用，关闭即释放麦克风流与 AudioContext
- **消除黑色实色底面**：云母模式里大量小面（选择器、菜单、气泡、提示、输入框、升降按钮、代码块、导航项等）原来用的是**不透明近黑 token**，读成一块块「黑色实色底面」与玻璃面板不搭；现把这些分层/特定/按钮/代码 surface token 全部改成暖调半透明（`rgba(...,0.5~0.72)`），流体/壁纸可从这些控件背后透出，整个界面不再有实黑块

## v1.0.0

**首个 Mineradio 主题构建**——基于 Aqua 玻璃拟态框架，把 Mineradio「私人视觉电台」招牌视觉全量移植进 DSH Web 界面。

- **香槟金调色板**：`#f4d28a` 香槟金 / `#7ad7c2` 薄荷 / `#ff5367` 余烬玫瑰，压在一层温暖的近黑 `#08090B` 演播室底色上，驱动全部 alias token、按钮、描边与滚动条
- **暖调阴影 + 香槟辉光**：可见阴影带金色辉光，鼠标辉光从暖琥珀扫到薄荷，保持品牌暖调
- **字体**：Inter + Noto Sans SC 自托管 Latin 脸（无 shell / fontsource 依赖）
- **流体背景默认暖金**：色相基准归零，默认 44° 落在香槟金；深度可调
- **完整设置面板**：插件卡片 + 外观行，双模式（云母 / 兼容）、模糊、磨砂、背景来源（流体 / 壁纸 / 视频）、环境装饰开关全部可调
- 图标与命名全面从 Aqua 迁移到 Mineradio

## v1.3.0（Aqua 基线）

- 视频壁纸全面升级：浏览器原生解码、无进度条、循环播放、铺满裁剪，窗口缩放实时自适应
- 视频壁纸「记住文件授权」：选一次即可（File System Access，刷新自动读取原文件、改动即时生效、不占额外存储；非 Chromium 自动退回数据库存储）
- 视频壁纸自动柔光罩 + 聊天气泡实底，保证文字在视频上清晰可读
- 视频壁纸新增「模糊度」「亮度」两个滑杆（含提醒文案）
- 发送栏与数据栏共用一块玻璃：单一背景/模糊/边框/投影，分界线精确落在左右圆角弯曲处，两片玻璃不再出现模糊度/清晰度不一致
- 数据栏文字、发送栏占位符改中性色（深色模式近白、浅色模式深灰），去除蓝色调
- 玻璃卡片平板化：去掉卡片底部黑色渐变，视觉更干净

## v1.2.1

- 鼠标辉光颜色跟随「色调」设置（与流体晕染色共用色相基准），不再默认泛蓝

## v1.2.0

- 新增「悬停下压」：官网卡片同款 3D 倾斜（哪边鼠标哪边下压、透视 800px + 微放大 1.01、0.1s 过渡），顶栏/侧边栏/发送框+数据栏/轨迹窗统一配方，轨迹窗幅度减半，设置里可开关
- 新增「鼠标辉光」：跟随光标的荧光青光晕，位于玻璃之下从磨砂中透出，不遮内容
- 设置页玻璃化：50px 磨砂、深浅色区分、兼容模式生效；打开设置即时弹出不再卡顿
- 设置分组优化：环境装饰（粒子鲸鱼/小鱼/网状交互）与悬停效果（鼠标辉光/悬停下压）分组，子标题放大加粗
- 背景新增「色调」「颜色深浅」双滑条：全程无极调节；0°/360° 锚定蓝色、默认 320° 青蓝；深浅 0 端直落纯黑（深色）/深浓原色（浅色），100 端浅淡粉彩
- 兼容模式修复：注入层不再顶歪顶栏/新建会话按钮/发送栏
- 浅色模式聊天文字微投影 + 1px 光晕
- 出厂默认：模糊 20 / 磨砂 7 / 色调 320 / 深浅 25 / 亮度 50，全部开关默认开启

## v1.1.2

- 新增「小鱼」开关（背景游动的小鱼/气泡，位于粒子鲸鱼开关右侧）
- 壁纸新增「删除」按钮（清除已选壁纸）

## v1.1.1

- 侧边栏收起状态悬浮玻璃，收起带渐隐动画
- 页面顶部/底部渐变模糊带（浅色泛白、深色泛黑）
- 漂浮玻璃投影加深
- 新建会话按钮透明玻璃，磨砂度跟随磨砂旋钮 +20
- 统计行与发送框分界线更清晰
- 设置页不再被顶栏遮挡；展开时 Harness 铭牌实时就位
- 设置文案简化

## v1.1.0

- 新增「粒子鲸鱼」：deepseek.com/harness 同款（官网粒子引擎 2D 移植），显示在聊天主区域正中央（不含侧边栏），深色模式白粒子、浅色模式灰粒子，设置里有开关可关
- 深色模式侧边栏铭牌换成官网同款「Harness」光泽药丸（135° 渐变描边 + 柔光 + 黑底白字），浅色模式保持原版铭牌
- 页面顶部/底部新增 5px 渐变模糊带，悬浮在聊天内容上层，内容滚到边缘渐入模糊；浅色模式微泛白、深色模式微泛黑
- 「漂浮玻璃」更名「云母效果」

## v1.0.8

- 通用设置里的玻璃主题控件去掉独立标题，直接跟在「外观」下方；插件页总开关关闭时，通用设置里的整块调节自动隐藏，打开开关即恢复
- 「选择图片」旁新增提示：浅色壁纸用浅色模式，深色壁纸用深色模式⚠️
- 设置排版统一：壁纸行改为「标签 + 控件」结构，模式/亮度/壁纸三条说明文字全部与所解释的控件列对齐

## v1.0.7

- 背景亮度自动跟随深浅模式：**深色模式只能在 0–50 调节**（压暗，50 原样），**浅色模式只能在 50–100 调节**（提亮，50 原样）；「跟随系统」按实际解析出的深浅色自动切换，旋钮范围与提示文字实时翻转，另一侧的旧值自动失效不干扰

## v1.0.6

- 设置重新布局：总开关留在「插件」页（卡片形状与其他插件一致），其余全部调节（模式/模糊度/磨砂度/流体颜色/背景亮度/背景/壁纸）移到**通用设置 → 外观**的正下方；主题未开启时该行会变暗并提示开关位置

## v1.0.5

- 修复「背景亮度」0–50 无效的问题：黑色遮罩原来画在流体/壁纸**下面**被盖住，现与白色遮罩合并到同一顶层（`::after`），0 = 死黑、100 = 死白、50 = 透明全部生效

## v1.0.4

- 新增「背景亮度」调节（0 = 纯黑、50 = 透明、100 = 纯白），漂浮/兼容两种模式都可用

## v1.0.3

- 安装器改为**优先 zip 下载**（普通 HTTP 更快更稳），git clone 只作后备——git 连 GitHub 不稳时不再白等十几秒

## v1.0.2

- 安装脚本默认安装**最新发布版**（通过 GitHub API 解析，可 `-Version 'v1.0.1'` 指定版本或 `-Version 'main'` 跟随开发分支）
- 修复安装脚本在 Windows PowerShell 下中文提示乱码（UTF-8 BOM）

## v1.0.1

- 兼容模式新增「背景流体颜色」调节入口（颜色旋钮在两种模式下都可用）
- 介绍补充兼容模式（双模式）说明

## v1.0.0

首个正式版本：可开关的玻璃主题，双模式 + 高自由度自定义。

- **漂浮玻璃模式**：顶栏、侧边栏、发送框、统计行、轨迹视图变成悬浮玻璃卡片，聊天区在流体上滑动
- **兼容模式**：保持原版排版一字不动，只把材质换成通用玻璃——按角色和类名子串命中菜单/卡片/气泡/下拉/提示等保守表面，其他插件的界面同样自动玻璃化
- **可调旋钮**（无极滑块 + 数值框）：玻璃模糊度、磨砂度、背景流体颜色
- **背景**：流体板或自定义壁纸（铺满页面、比例不变），壁纸单独可调模糊度/磨砂度，图片自动压缩后本地保存
- 深色玻璃改中性冷灰，磨砂度拉满不再泛蓝；发送栏正确模糊背后的内容
- 问候语等文案功能独立，不在本主题内
- 一键安装 `install.ps1`（按版本号安装，无 git 时自动退回 zip 下载）
